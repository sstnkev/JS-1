'use strict'

const evenNumbers = [2, 4, 6, 8, 10]
const oddNumbers = [1, 3, 5, 7, 9]
let concatNumbers = evenNumbers.concat(oddNumbers)
console.log(concatNumbers);
console.log(concatNumbers.indexOf(8));
